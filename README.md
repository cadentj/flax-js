> Note: This library depends on a fork of `jax-js` which you can find [here](https://github.com/cadentj/jax-js).
>
> Clone the repo, run `pnpm build`, then update the `package.json` to point to that repo. 
>
> TODO: Make a PR or add as a submodule for now.